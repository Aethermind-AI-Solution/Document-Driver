# Dev/test-only dependencies (not needed at runtime, kept out of requirements.txt
# so production images stay lean). httpx is required by fastapi.testclient.TestClient.
-r requirements.txt
pytest>=8,<9
httpx>=0.27,<1
